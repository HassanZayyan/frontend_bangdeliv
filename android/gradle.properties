org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Flutter plugins are cached on C: while this workspace is on D:.
# Kotlin incremental caches cannot relativize sources across those roots.
kotlin.incremental=false
# Never commit API keys here. Inject MAPS_API_KEY from local machine or CI.
